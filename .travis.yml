language: node_js

node_js:
  - '8'

install:
  - npm install

cache:
  directories:
    - node_modules

deploy:
    - provider: script
      script: bash scripts/deploy.sh
      on:
        all_branches: true

